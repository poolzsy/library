<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lilac.mapper.RoleMapper">
    <insert id="save">
        insert into role(role_name, role_key, remark, status, del_flag, create_time, update_time)
        values (#{roleName}, #{roleKey}, #{remark}, #{status}, #{delFlag}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        update role
        <set>
            <if test="roleName != null">
                role_name = #{roleName},
            </if>
            <if test="roleKey != null">
                role_key = #{roleKey},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
    </update>

    <select id="page" resultType="com.lilac.domain.entity.Role">
        select * from role
        <where>
            del_flag = 0
            <if test="roleName != null and roleName != ''">
                AND role_name like concat('%', #{roleName}, '%')
            </if>
        </where>
    </select>
    </mapper>