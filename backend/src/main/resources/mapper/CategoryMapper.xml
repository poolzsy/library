<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lilac.mapper.CategoryMapper">
    <insert id="save">
        insert into category (category_name, status, description, update_time, create_time, del_flag)
        values (#{categoryName}, #{status}, #{description}, #{updateTime}, #{createTime}, #{delFlag})
    </insert>

    <select id="page" resultType="com.lilac.domain.entity.Category">
        select * from category
        <where>
            del_flag = 0
            <if test="categoryName != null and categoryName != ''">
                AND category_name like concat('%', #{categoryName}, '%')
            </if>
        </where>
    </select>

    <update id="update">
        update  category
        <set>
            <if test="categoryName != null">
                category_name = #{categoryName},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="delFlag != null">
                del_flag = #{delFlag},
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>